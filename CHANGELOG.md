# 更新日志

## [Unreleased]

## [1.13.6] - 2026-09-06

### 真实 bug 修复批(全模块排查)
- **对话时序(彻底修)**：1.13.4 的渲染层修法对「模型不说话直接调工具」这一最常见场景无效——结论文字流进了回合开始就创建的占位消息。现改为数据层修复：工具先到时作废空白占位段，结论懒创建为工具之后的新消息，流式进行中即正确，历史记录也不受影响。
- **流式中删消息会写错行/崩溃**：回合进行中删除任何消息会让流式写入的下标漂移(答案写进别的气泡、工具结果挂错卡片、极端时越界崩溃)。现流式期间一律拒绝删除(与上下文压缩的既有保护一致)。
- **思考内容跨段重复**：多段交错回合里,后段会重复显示前段已展示过的思考——现在每段只带自己的新增思考。
- **MCP 鉴权头明文落库**：远程 MCP 的 Authorization 头(含 GitHub OAuth Token)此前明文存进数据库,现统一 Keystore 加密存放,旧版明文存量自动兼容。
- **启动重连失败状态失真**：离线启动时 MCP 重连失败,页面仍显示「已连接/已安装」——现失败即回落为未连接,插件卡片可一键重装。
- **MCP 重连泄漏**：同一地址重复连接时旧传输未关闭(本地 stdio 形态会孤儿化子进程)——现重连前先关闭旧连接。
- **杂项**：插件页加载完成前不再闪「已安装 0 个」、按钮加载中禁用；纯工具步骤不再渲染一排空操作按钮；产出文件抽屉在图片生成完成时即时刷新；空会话不显示产出文件入口。

## [1.13.5] - 2026-09-06

### 修复与思考样式对齐 Claude
- **修复**：v1.13.4 中设置页「插件」入口被误删的问题——现已放回「Agent 工具」分区第一行，打开设置即可见。
- **思考样式对齐 Claude**：收起时不再带底框，改为一条浅色摘要行（时钟图标 + 思考内容首句摘要 + 细箭头），点开才展开完整思考内容；无思考内容时回退显示「思考了 X 秒」。

## [1.13.4] - 2026-09-06

### 插件市场与对话体验重构
- **去重**：设置页撤下「Git 接入」入口（其 GitHub 授权能力由插件页「GitHub 连接器」承接，Token 同源共用）；撤下「搜索 API Key」设置项。
- **插件市场**：目录扩至 12 个插件，新增 Microsoft Learn、Context7 两个免鉴权官方 MCP（装完即用，无需自填地址）；页首新增「共 N 个插件 · 已安装 M 个」总览；Composio 安装弹窗改为三步引导并附控制台直达链接。
- **官方图标**：GitHub 连接器/远程 MCP 使用真实 octocat 标志，Microsoft Learn 使用微软四格标，替代通用图形图标。
- **对话时序修复**：最终结论不再排在工具卡上方——渲染按消息真实发生顺序拆分，先于结论发生的工具排在前面，结论落在工具之后；历史会话记录同样修复。
- **对话减负**：工具卡默认收起成一行摘要（点击展开详情），成功状态不再自动展开；计划卡默认折叠；多步工具统一收进「执行了 N 步」折叠区。
- **思考折叠**：仿 Claude——默认只显示一行「思考了 X 秒」摘要条，点开才展开思考内容。
- **对话产出（Artifacts）**：对话顶栏新增文件图标，底部抽屉列出本会话生成的 MD/文件/图片（来自真实的写文件/下载/生图工具调用），点按条目复制路径。
- **页面美化**：身份卡改为头像字卡片 + 「● 使用中」徽章；精编记忆改为圆角卡片编辑区 + 药丸保存按钮；看板列头加色点与计数徽章、任务卡升级为描边卡片、弹窗输入框圆角统一。

## [1.13.3] - 2026-09-05

### 插件商店（设置 → Agent 工具 → 插件）
- **插件入口**：设置页「Agent 工具」新增「插件」入口，进入整套插件页面：按连接器 / MCP 服务 / 技能包三类展示，卡片带实时安装状态，一键安装/卸载，卸载有确认弹窗。
- **连接器**：GitHub 连接器走 OAuth 设备流——授权弹窗内展示用户码并自动跳转浏览器完成登录，结果自动回传，Token 经 Keystore 加密存储，与「Git 接入」共用同一份授权。
- **MCP 服务**：GitHub 远程 MCP（官方端点，免 root/免 node）、DeepWiki（公共 MCP，查开源仓库结构化 Wiki）、Composio（粘贴控制台生成的 MCP 地址，接入 500+ SaaS 应用的托管授权）。安装即真实握手并把工具注册给 Agent，卸载即注销工具。
- **技能包**：安全代码审计、逆向分析、APK 构建、APK 更新管理、APK 反编译、LSPosed 模块开发 6 个内置技能转为可装卸插件。
- **授权跳转弹窗**：需要账号授权的插件，安装时先弹「跳转授权」弹窗（用户码 + 跳转浏览器 + 自动检测完成），申请失败/授权超时/用户拒绝均有明确提示。
- **修复**：内置技能重导入不再把已卸载（归档）的技能复活；归档技能不再注入系统提示词，卸载即刻对 Agent 生效。

### 生图兼容性修复
- 仅使用供应商实际公开的生图模型，失败后停止 Agent 盲目改猜聊天模型。
- 兼容 GPT Image 的 `output_format`、DALL-E 的 `response_format`、Responses `image_generation` 返回格式。
- 生图成功后工具卡直接显示原始图片字节，历史消息也可恢复显示；下载图片时不向第三方 URL 转发 API Key。

## [1.13.2] - 2026-09-05

### 侧边栏 / 终端 / 设置 / 计费 / 生图集中修复
- **侧边栏**：去掉「工作台」小标题，像素指挥室/终端/群聊房间上移至 IDE 下方；目标模式/MCP/Skills 移出侧边栏；XINCODE 标题上移；头像字改为可设置的个人昵称（设置 → 个人昵称）。
- **终端**：配色跟随整体橙色系主题；运行中输入框可用，回车直发进程 stdin（Y/N 可答）；apt 安装自动补 `-y` + 非交互环境；终止加固（整组 + 子进程多轮 KILL）；IDE 各页终端日志绿字统一为主题橙。
- **界面语言**：新建中英字典并接入主要页面（侧边栏/聊天/设置/终端/列表/供应商/用量/身份卡/Skill-MCP），切换秒生效。
- **设置**：「模型委托」并入「功能模型配置」（页内可配自定义端点覆盖）；新增身份卡入口与个人昵称设置；权限分级查询改为异步可反馈，并修复 Root 状态不刷新。
- **用量成本**：两处价格表按 2026-01 官价重写（GPT-5/4.1/o 系、Claude 4 系、Gemini 2.5/2.0/1.5、Grok 4、Mistral，美元×7.2）。
- **供应商页**：新建配置的供应商/API 路径下拉改覆盖层，下拉不再带整页滚动；模型徽章与选中色统一橙色系；Skill/MCP 选择弹窗统一样式。
- **Agent 生图**：新增 `generate_image` 工具（OpenAI `/images/generations` 兼容），PNG 原图保存不压缩，助手气泡直接渲染；功能模型配置可单独绑定生图供应商。

## [1.13.1] - 2026-09-05

### 原功能入口恢复与体验修正
- **侧边栏**：主导航第三项改回 `IDE`（只借 Claude 排版样式，不照搬 Artifacts 命名）；新增「工作台」分组，恢复像素指挥室、终端、目标模式、群聊房间、MCP、Skills 六个原功能入口。
- **设置页置顶**：推翻按点击次数统计的“常用置顶”，改为用户手动置顶——每项右侧 ☆ 置顶到顶部「★ 我的置顶」，点击直达。
- **加号抽屉**：`Connectors` 改回 **MCP**（显示已配服务数）、新增 **Skills** 行（显示已配技能数），另保留「工具权限」行。
- **思考强度**：Effort 档只剩 Low / Medium / High / Extra / Max，去掉 `Default` 与 `1.5× or more usage` 订阅倍率徽章；模型默认项与工具权限描述同步去 Claude 化。
- **模型面板**：Effort 卡下方新增「更多设置」区，补回访问权限（含完全访问）、上下文用量、增强提示词、目标模式、多Agent协同五个原核心功能入口。
- **设置页**：外观新增「界面语言 / Language」中文 / EN 切换开关，持久化保存。

## [1.13] - 2026-09-04

### Claude 移动端全链路美学复刻与布局重构
- **侧边栏彻底对齐 Claude 移动端**：
  - 顶部简洁优雅的古典过渡衬线标题 `XINCODE`，移除割裂的工作区与多余间距。
  - 主导航一级入口：`Chats` / `Projects` / `IDE`（1.13.1 起改回原功能名）。
  - 内容流组织为三大优雅分组：`Projects`、`Pinned`（带小图钉）、`Recents`（带圆点标记与 `2026年X月X日` 格式日期）。
  - 彻底移除侧边栏底部突兀的搜索框（统一由 Chats 管理页与 Projects 承载）。
  - 底栏重塑：左侧为个人设置徽章入口，右侧并排紧凑型黑色椭圆小胶囊 `+ New chat`。
- **对话界面输入岛与模型/思考选择深度对齐**：
  - 彻底清除输入框内部与顶栏多余的零散切换按钮（去除重复的模型切换、模式选择、灵感提示等冗余图标）。
  - 纯净悬浮输入岛：多行舒适输入区，提示文本精准对齐 `Reply to Claude...`；底部仅保留左侧圆圈 `+` 号、模型与思考强度胶囊（如 `Sonnet 5 High`）以及右侧语音 🎙 与 发送 ↑ / 停止 ■ 按钮。
  - **完整复刻多级抽屉交互**：
    - `Add to chat` 抽屉：上排 `Camera`、`Photos`、`Files` 三大功能大卡片；下方列表包括 `Web search`（带开关）、`Memory`（带开关，副标题标注不可修改）、`Add to project`（显示当前项目，点击滑入选择抽屉）、`Tool access`（显示权限模式，点击滑入权限抽屉）、`Connectors`（MCP 与技能接入）。
    - `Select model` 抽屉：顶部带 `✕`，列出模型名称、Pro/Max 徽章、场景说明与右侧选中深蓝对勾 `✓`；底部常驻 `Effort` 卡片带时钟思考图标与当前档位。
    - `Effort` 抽屉：顶部带 `←` 返回，列出 `Low`、`Medium`、`High (Default)`、`Extra`、`Max (1.5x or more usage)` 档位，实时切换并回显。
    - `Tool access` 抽屉：提供 `Auto`、`On demand`、`Always available` 三档标准模式并支持单选。
    - `Add to project` 抽屉：顶部带 `←` 返回，自带 `Search projects` 搜索框，点击项目一键关联。
    - 会话顶部 `⋮` 菜单：展示当前会话标题，支持 `Share`、`Rename`、`Pin`、`Add to project`、`Delete`。
- **复刻独立管理页面**：
  - `Chats` 会话管理页：大号 Serif 标题 `Chats`，自带搜索框，右上角支持置顶筛选与多选模式，多选状态下可批量删除，右下角常驻 `+ New chat` 浮动胶囊。
  - `Projects` 项目详情页与编辑抽屉：进入项目展示大标题、项目描述、`🔒 Private` 锁标、Memory 状态卡片、Project knowledge 与 Custom instructions 双卡片，列出项目内会话列表；点击 `Edit details` 弹出 `What are you working on?` 与 `What are you trying to achieve?` 模态表单与黑色 `Save` 按钮。

### 图片发送与多模态视觉 Bug 彻底修复
- **输入框实时缩略图预览**：选择照片或拍照后，在输入岛上方渲染带 `✕` 删除角标的高清缩略图卡片，随时预览与撤销，不再只记录文件路径。
- **对话流图片真实渲染**：彻底修复对话气泡中只显示本地 Linux 路径的技术文本 Bug。气泡内自动提取图片并解码展示圆角大图，点击可直接查看大图，并剥离冗余技术路径。
- **多模态模型视觉透传**：在生成 API 请求时，图片会自动编码为标准 Base64 `image_url` 图像块，支持 GPT-4o、Gemini、Claude、Qwen-VL 等视觉模型原生识别，非视觉模型亦提供清晰提示调用 `describe_image`。

### 设置页面与终端工具优化
- **设置页面滑动位置持久化记忆**：修复退出子页后返回设置页跳回顶部的痛点，自动记忆并恢复离开时的滑动偏移量。
- **设置项搜索与隐藏**：顶部常驻设置项搜索框，支持关键字实时过滤；增加「自定义隐藏项」面板，用户可自由勾选隐藏不常用的功能区域。
- **常用功能置顶**：自动统计设置项点击频率，在设置主页顶部展示「★ 常用置顶」快捷入口。
- **终端工具大幅强化**：整合并放大 SHELL 执行/终止按钮；去除混淆的顶部快捷输入框，新增横向滑动的「快捷指令栏」，内置常用系统排查命令，支持将当前命令「收藏当前」并持久化保存，点击一键执行。

### 供应商、模型与 MCP 稳定性
- 修复供应商配置编辑/删除按钮触控区域过近导致的误触，两个操作现在拥有独立的 40dp 触控区域和清晰间距。
- 重做模型发现：支持 OpenAI 兼容的 data 响应、Google Gemini 的 models 响应、Gemini OpenAI 兼容入口和 x-goog-api-key 鉴权；在线列表成功后替换旧缓存，避免只显示过期默认模型。
- 修复 Google Gemini 工具调用缺少 thought_signature：签名会随流式响应、assistant tool_calls、断点游标、Room 工具记录和恢复历史完整保留。
- 每个模型可单独配置上下文窗口、最大输出 token、思考强度和图片输入能力；数据库增加 v46→v47 迁移。
- 供应商市场增加搜索和侧边分类筛选。
- 合并 PR #5 的 mobile-agent-prompts 专用提示词库及 32 个工具 Schema。

## [1.12.3] - 2026-09-04

### Claude 桌面端美学重塑与排版彻底解放
- **彻底解除 AI 正文等宽代码体硬编码**：全面重构 `MarkdownContent.kt`，普通文本与列表项全面切换为高舒适度的人文无衬线阅读体（`XinUiFont`，行高扩大至 22sp），各级标题全面采用古典过渡衬线体（`XinSerifFont`），彻底摆脱死板代码终端感，呈现如同装帧期刊一般的沉浸阅读质感。
- **对话流去“方盒子化”**：AI 助手回答打破原有的沉重封闭灰黑方框，升级为通透舒展的书刊式直接排版，搭配精致的赤陶星芒标识；用户消息重构为 20dp 大圆角暖调卡片。
- **实装 Claude 欢迎英雄区（Greeting Hero）**：会话初始不再是空白虚无，而是展示温雅的问候语（“下午好，探索者” / “今天想探索什么？”）与 4 组高频工程探索胶囊卡片，点击一键填入。
- **悬浮胶囊输入岛（Floating Capsule Island）**：底部输入栏全面悬浮化，配有柔和微描边与大圆角，发送按键升级为 Claude 经典实心赤陶珊瑚色圆形上箭头按钮（↑）。

### 供应商管理、导航与生态库完善
- **合并 PR #5 专用工程提示词库**：集成 `mobile-agent-prompts/`，包含系统级基座提示词、Plan Agent 规划调度提示词、Explore Agent 代码分析提示词以及 32 个核心工具的标准 JSON Schema 定义。
- **修复供应商配置“新建”按钮缺失**：列表顶部常驻「+ 新建配置」按钮，无配置时展示引导空状态卡片，随时一键添加。
- **收敛设置页多余入口**：合并“免费模型”与“供应商配置”双入口为「模型与供应商」单一枢纽，配置卡片支持快捷一键切换运行模型。
- **导航栈与本地网络连接**：侧边栏 5 大核心功能返回精准回到聊天主页；放行本地 `http://127.0.0.1:8787/mcp` 明文 HTTP 连接，支持本地扩展服务。

## [1.12.2] - 2026-09-03

### UI 风格与供应商交互重大优化
- **修复供应商配置“新建”按钮缺失**：无论是否内嵌在 Hub 中，配置列表顶部与空状态卡片均常驻「+ 新建配置」按钮，点击随时创建。
- **收敛设置页重复入口**：将原设置中的「免费模型 / 供应商」与「供应商配置」合并为单一的「模型与供应商」，直通包含“我的配置”和“供应商市场”的双 Tab 整合页，消除歧义。
- **全面引入 Claude 桌面端美学风格**：
  - 色彩系统重塑：采用 Claude 标志性的 Warm Parchment（暖米纸色 #FAF8F5）与 Warm Obsidian（暖曜石黑 #181715），搭配 Claude 标志性陶土珊瑚色（Terracotta Coral #CC6644 / #E07A5F）品牌强调色与暖金琥珀色，边框极为细腻温润。
  - 优雅字体层级：引入古典文学衬线体（Serif）用于品牌标志、会话大标题和界面大标题，搭配温润清爽的无衬线正文，呈现如同 Claude 客户端一般静谧沉浸的阅读感。
  - 控件质感提升：圆角与内边距呼吸感提升，返回按钮升级为圆形轻量化交互，Header Action 升级为 Claude 风格圆角药丸按钮。

## [1.12.1] - 2026-09-03

### 体验与路由修复
- **侧边栏 5 大功能返回首页**：修复从侧边栏进入 IDE、MCP、Skills、群聊房间、Goal 模式后，按返回键或点击顶栏返回误跳至设置页的路由问题，现统一精准返回聊天主页（chat）。
- **本地明文 HTTP MCP 支持**：新增 Android 网络安全配置（Network Security Config），放行 `http://127.0.0.1:8787/mcp` 及 `localhost` 等本地回环明文连接，解决 Android 9+ 阻止 cleartext HTTP 导致的 MCP 本地连接失败。
- **供应商快捷更换模型**：在供应商管理卡片上新增「更换模型」快捷操作入口，点击即可直接切换已启用的模型并实时刷新生效。

## [1.12]

APK 版本为 `versionName 1.12`、`versionCode 122`。

### Agent 核心与模型能力

- 接入 OpenAI Responses API：支持 `/v1/responses`、流式事件、函数工具回环、结构化输出、辅助模型、Goal 裁判、群聊与提示词扩写。
- 支持多供应商与自定义模型：可在当前对话内独立切换供应商和模型，支持刷新模型列表与手动填写模型 ID，删除配置后自动安全回退。
- 完善工具调用循环、SSE 增量聚合、截断检测、错误透传与模型能力声明，减少错误重试、无响应和“未知工具”死循环。
- 支持视觉识别、深度推理、翻译、语音转写等功能模型委托，并明确委托模型与供应商配置的优先级。

### 群聊与多智能体协作

- 群聊支持成员并行回复、消息引用、中文 `@` 识别、成员互相接棒与连锁跳数控制。
- 每个成员拥有独立工作台和模型配置，成员可在共享房间工作区中调用工具、读写文件、执行命令，群聊只展示最终汇报。
- 成员工作回合、工具调用、工具结果和最终输出稳定保序；支持流式回复、实时过程卡片、工具确认、打断、重发、复制和引用。
- 群聊滚动总结降低长期上下文成本；房间拥有独立工作区、工作会话和记忆作用域，产品团队记忆不会泄漏到普通 Agent。
- 预制产品团队支持秘书助理、产品经理、架构师、工程师、前端设计师和测试工程师等角色及专属技能。

### 记忆、技能与自动化

- 长期记忆支持 FTS 全文检索与向量重排，按问题相关性召回；记忆写入串行化、按来源去重并记录召回统计。
- 用户画像与近况支持后台复盘维护；技能增加使用统计、自动复活、定期自我改进和 active/stale/archived 生命周期管理，内置技能保持只读保护。
- Goal/Work 多任务支持后台持续执行、自动领取、失败重跑、验收状态、执行记录和完成通知；定时任务支持 cron/WorkManager。
- MCP 支持 stdio 本地传输与 HTTP 远程连接，修复并发访问和连接泄漏问题。

### 手机端 IDE、环境与终端

- 手机端可直接创建项目、运行 Gradle、切换 JDK 11/17、管理 SDK/NDK，并按终端与构建分别注入环境变量。
- 内置 Ubuntu 环境支持 root/chroot 自动部署，补齐挂载、清理与 `/sdcard` 项目访问；普通用户无 Root 时仍可使用 Git 和文件能力。
- 新增 Root、Shizuku、ADB 分级执行：按 `Root > Shizuku > 普通` 自动降级，终端统一接管执行路径。
- 默认工作区迁移到应用自身可写目录，普通文件与 Shell 使用应用 UID；只有显式 `su_exec` 才请求 Root。
- 代码索引内核基于 tree-sitter，支持多语言增量索引、符号定义与调用关系查询；ARM64 动态库加载问题已修复，无法加载时仍可回退文本搜索。
- 侧边栏重组 `DEVELOPER` 入口，统一进入 IDE、MCP 和 Skills；环境配置集中管理 Gradle、JDK、SDK 和环境变量。

### 安全、稳定性与数据保护

- 文件工具和 Shell（包括 Root 路径）禁止访问应用数据库、SharedPreferences 等运行时私有数据，避免 AI 或命令误改导致数据不可读。
- 数据库打开失败时先尝试修复权限，只有确认无法恢复才进入备份重建流程，并向用户说明数据与权限的区别。
- 完善敏感操作确认、权限模式、审计日志、输出有界截断与超时控制，修复 Shell stdout/stderr 管道死锁、无限等待和进程组残留。
- 修复数据库迁移、索引、重复键、Room 校验、Compose 状态线程、模型供应商路径及 Judge 平票等稳定性问题。
- 修复群聊中的 `null` 文本、内部引用标签、工具协议标记与异常控制符泄漏。
- 供应商 Hub 页面签名对齐、侧栏 GOAL 入口接线、keystore 缺键中文明示；密钥解密失败不再把密文当 Key 发送；移除静默删库逻辑，FTS 损坏时降级 LIKE 并提示；GoalRunner 忙态与竞态收敛；流式丢事件计数上报；MCP 连接调用加超时；路径规范化防绕过；定时任务改期生效；设置页入口去重。

### 对话体验与界面

- 主聊天与群聊支持更完整的 Markdown：标题、粗体、列表、行内代码、表格和可点击链接。
- 图片发送使用系统 Photo Picker，并通过应用私有目录按需处理，不要求额外存储权限。
- 收窄消息留白、扩大气泡可用宽度，群聊成员显示供应商头像；设置页面支持分组折叠、图标和权限状态卡。
- 上下文圆环、token 用量、缓存命中率和人民币成本显示持续可用，并修正缓存 token 计费口径。
- 深色模式覆盖所有普通页面，保留终端与指挥室的专用配色；修复屏幕旋转、输入框溢出、引用块重叠等界面问题。

### 兼容性与升级说明

- 本次为 1.12 正式版本更新，包含当前代码基线的全部功能与修复。
- Android 构建目标保持 `minSdk 28`、`targetSdk 34`、`compileSdk 34`，正式包仍不内置任何 API Key。
- 数据库包含自动迁移逻辑；升级前建议保留应用数据备份，尤其是涉及旧版本长期记忆、供应商配置和群聊房间的设备。


## [1.11]

**会话隔离与团队群聊可靠性**

- 新建对话不再自动塞入身份卡的开场白；群聊专属身份不能再污染普通聊天，历史误选状态会自动回到默认助手。
- 产品团队的运行任务移到应用级生命周期，离开群聊页面后仍会继续执行；返回房间可继续查看进度或停止任务。
- 成员工作台只显示用户可见的助手输出，角色系统提示词、内部调度消息和原始工具 JSON 全部留在底层。

**提示词扩展器**

- 模板改为可多选，组合内容可继续编辑，也可以在生成途中再次点击灯泡取消。
- 扩展请求使用严格的结构化边界；模型若返回普通聊天回复而不是扩展结果，会明确失败而不会覆盖输入框。

**ARM64、工作区与 Root 边界**

- Android 代码索引内核不再链接 Node/N-API 注册层，ARM64 动态加载失败的根因已移除。
- 默认工作区迁移到应用自身可写目录，普通文件和 Shell 操作始终以应用 UID 运行；只有显式 `su_exec` 才请求 Root。
- 旧的共享存储工作区会迁移到新的免 Root 工作区，代码索引错误页会显示真实 ABI 与加载原因。

## [1.10]

**严重:AI 能把应用自己的数据库搞坏,导致全部数据丢失**

有用户让 AI 自行安装技能,AI 在过程中动到了应用自己的私有目录,下次启动报
`Cannot open database ... xincode.db is not readable`,应用只能重建一个空库 ——
会话、身份卡、供应商配置、记忆全没了。设备 root 之后尤其容易踩到:`shell_exec` 会自动走 root,
一个 `chmod`/`chown` 落在那个目录里,文件就变成 root 所有,应用自己反而读不了。

注意这种情况下**数据一个字节都没坏**,坏的只是文件权限。三处都改了:

- **禁止 AI 读写应用私有目录里的 `databases/`、`shared_prefs/` 等运行时数据**,
  文件工具和 shell(含 root)都拦。内置 Ubuntu 环境所在的 `files/` 不受影响,照常可读写。
- **打不开时先尝试修复权限再重试**,修得好就原样打开,不再一上来就把库废掉重建。
  (被 root 夺走归属的情况修不了,仍会走备份重建。)
- 真的走到重建时,提示框会说明这是权限被改动造成的、数据本身没坏,而不是只甩一句报错。

## [1.09]

**成员工作台搬进群聊内部**
- 点消息上方的名字就能展开那个人的工作会话,看他这会儿在做什么,返回即回群聊。
- 此前它是「跳到主对话页去找那条会话」—— 点一下人就离开了群聊,回来还得自己找回去。

**工具失败大修**

这一批基本是同一类问题:工具其实能用,只是模型第一次没调对,而错误提示没给出改对的办法,
于是它换着花样重试,连错三次就被防空转刹车掐掉,整轮任务作废 —— 用户看到的是一屏 `exit -1`。

- **`execute_code` 在真机上从来没成功过**。脚本桥用反射把 Kotlin 对象接进 JS,而这条路会触发
  Rhino 内部一段引用了 `javax.lang.model` 的初始化 —— Android 上根本没有这个包,于是每次都抛
  `NoClassDefFoundError`。改成完全不做反射的原生实现,顺带砍掉了脚本从 JS 摸到任意 Java 类的入口。
  **这个 bug 在电脑上跑测试复现不出来**,只在手机上炸。
- `execute_code` 只能跑 JavaScript,但模型常传 `language: "python"` 再写一段 Python。
  现在直接说清楚是语言选错了,并指出该改用哪个工具。
- **工具名写反了也能认**:`search_web` → `web_search`、`read_file` → `file_read`。
  只在唯一命中时才纠正,认不出来宁可报错也不猜。纠偏发生在权限判定之前,
  不会出现「按 A 判权限、拿 B 去执行」。
- **联网搜索关着时会说人话**:此前只是把工具藏起来,模型就一路猜 —— 先 `search_web`,
  再把界面开关当技能调 `invoke_skill("联网搜索")`,每次只拿到一句「未知工具」。
  现在明确回「这个开关关着,别再换名字试了,让用户去打开」。
- 顺带修了一个**开关不算数**的问题:藏 schema 挡不住模型照抄历史里的调用,
  联网搜索关着时照样能真发出去。现在前置条件不满足的工具一律不执行。
- `agent_plan` 说「这步做完了」不带步骤号不再报错 —— 默认就是当前进行中的那一步。
- `dispatch_agents` 原先只认一种参数写法,派一个人时写成对象、或键名写成 `tasks` 都会被拒。现在都认。
- `invoke_skill` 找不到技能时会列出现有技能名,并说明「联网搜索」这类是界面开关、不是技能。
- 未知工具的报错会先给出最像的几个名字,而不是甩一整串工具名。

**群聊成员干活的方式**
- 硬性要求「说一句 → 做一步 → 说结果 → 再做下一步」,不许先写一大段计划再一口气把工具全调完 ——
  那样看到的只有一堵文字和底下一串操作,中途出问题也认不出是哪一步。
- 讲清楚 **@ 只在最后一段汇报里有效**:过程中间 @ 谁都不会真的叫醒他,因为进群的只有最后那段。
  此前成员在工作会话里 @ 了人,群里没有任何人应答,看着像他说完就没下文了。
- **工作区此前只写在提示词里,没真的切过去**。成员写相对路径会落到全局工作区根,文件写不进去,
  而错误只有一句 `exit -1` —— 模型只能瞎猜(实测它会退到 shell 里 `chmod` + `cat >` 硬绕)。

## [1.08]

**代码索引(新)**
- 集成 [codegraph](https://github.com/colbymchenry/codegraph)(MIT)的 tree-sitter 抽取内核,
  支持 20 种语言。AI 想知道「某个函数在哪定义、谁调用了它」时,一次查询就出 `file:line` + 签名,
  不用把文件读进对话 —— 手机上上下文本来就紧张,这个差别很明显。
- 基于语法树而非文本匹配:不会像 grep 那样命中注释和字符串里的同名词。
- 设置 →「代码索引」建索引。增量更新(只处理改过的文件),跳过 `.git`/`node_modules`/`build`
  等目录和超过 1MB 的文件,可中途停。
- 内核加载不了时(非 arm64 设备)AI 自动退回 grep,功能不受影响。
- **APK 因此从 65MB 涨到 100MB。**

**群聊房间**
- 输入框左侧 `@` 按钮呼出成员卡片,含「@所有人」;`@所有人`/`@全体`/`@大家` 都认。
- **成员之间可以互相 @**,讨论能自己往下推进,不用你一句一句点名。可设连锁跳数上限或「不限」。
- 跑起来后随时可停;顶部显示当前是谁在说话。
- **完全访问**:成员可调用工具联网、读写文件、执行命令。
- **每个成员有独立工作会话**:干活的全过程留在自己那条会话里,群里只出现最后的汇报。
  成员列表点「工作台」跳过去看实时进度。
- **房间工作区**:默认 `<工作区根>/rooms/<房间名>/`,成员之间共享。
- 每个成员可单独指定供应商与模型。
- 群聊搬进侧边栏 —— 它是一种对话,和主对话、Goal 任务同一层。

**预制产品团队**
- 一键装好六个角色:秘书助理 / 产品经理 / 架构师 / 工程师 / 前端设计师 / 测试工程师。
- 每张身份卡写死了「盯什么 / 不管什么 / 什么时候闭嘴 / 该找谁」——
  只改名字不改设定的话,六个成员会给出六份雷同的泛泛而谈。
- 各配专属技能:会议纪要 / 写PRD / 技术方案 / 工时评估 / 交互方案 / 测试用例。

**扩展提示词**
- 四处入口:身份卡编辑、主对话输入框、群聊输入框、群聊加成员时按名字现造身份卡。
- 只写「架构师」三个字也能点,它会补出角色边界、关注点、输出要求;
  「做个记账 APP」会补出场景、约束、验收标准、不做什么。
- 关键是扩展**有结构**:让模型「写详细点」只会得到同一句话的注水版,
  所以每种场景都给死了产出骨架。

**六个通用工作技能**
- 结构化写作 / 画图表 / 查错 / 审查改动 / 拆任务 / 解释清楚。
- 不绑角色,任何身份、任何房间、主对话都能调。自建群聊加成员时可勾选。

**新增工具**
- `code_graph` —— 查符号定义、调用关系、文件结构。
- `delete_file`、`make_directory` —— 此前只能靠 shell 拼 `rm`/`mkdir`,意图不显式。
- `download_file` —— 与 `web_fetch` 分工:抓正文进上下文,下载存字节**不进**上下文。
- `sleep` —— 轮询外部状态时的间隔。
- `get_memory_by_title` —— `recall_memory` 给的是截断摘要,认出是哪条后要取全文。

**界面**
- 主对话与群聊都加了对话气泡,自己的靠右、对方靠左。
- 身份卡分场景:群聊角色卡不再混进主对话的身份列表。

**重要修复**
- **Markdown 只渲染了代码块**:`**粗体**`、`# 标题`、`- 列表`、`` `行内代码` ``、`[链接]()`
  的符号全都原样显示在界面上。现在完整支持,含表格与可点链接。
- **中文 @ 判不出来**:边界规则要求 @ 前面是空白或括号,于是「他问你呢@前端设计师」
  判成不命中 —— 用户 @ 了没人应,群聊看着像卡死,而日志里什么错都没有。
  根因是判断用了 `isLetterOrDigit()`,而汉字在它眼里也是字母。
- **群聊不记账**:群聊不走主客户端,完全绕过了用量记录 —— 聊半天烧掉一大笔,
  用量分析却显示「还没有用量记录」。
- 用量记账写库失败以前完全无声;只有缓存读、输入输出都是 0 的记录会被当空帧丢掉
  (Anthropic 缓存命中就长这样,那是真花过钱的)。用量页空着时现在会区分
  「压根没记上」和「不在这个时间范围内」。
- **分配模型时只显示一个**:可选模型只从「已启用」列表读,而那并不代表供应商提供了什么。
  现在可刷新拉取,也可手填。
- 通用技能的装载移到了初始化开关之前 —— 老用户的首启标志早已置位,
  放在里面的话他们永远装不上新技能。

## [1.07]

**群聊房间大改**
- 输入框左侧 `@` 按钮呼出成员卡片,含「@所有人」;`@所有人`/`@全体`/`@大家` 都认。
- **成员之间可以互相 @**。此前成员回复里的 @ 是死的,谁都不接话,讨论必然断在第一轮
  (秘书说完「大家聊聊」就没下文了)。现在讨论能自己往下推进,可设连锁跳数上限或「不限」。
- 跑起来后随时可停;顶部显示当前是谁在说话。
- **完全访问**:成员可调用工具联网、读写文件、执行命令。
- **每个成员有独立工作会话**。干活的全过程留在自己那条会话里,群里只出现最后的汇报 ——
  否则工程师贴一张工时表就把讨论淹了。成员列表点「工作台」跳过去看实时进度。
- **房间工作区**:默认 `<工作区根>/rooms/<房间名>/`,成员之间共享 ——
  架构师写的方案工程师要读得到。
- 每个成员可单独指定供应商与模型:秘书只是记录,便宜模型够用;架构师和测试值得上贵的。

**预制产品团队(一键装好)**
- 六个角色:秘书助理 / 产品经理 / 架构师 / 工程师 / 前端设计师 / 测试工程师。
- 每张身份卡都写死了「盯什么 / 不管什么 / 什么时候闭嘴 / 该找谁」。只改名字不改设定的话,
  六个成员对同一个需求会给出六份雷同的泛泛而谈,除了六倍花费没有任何增量。
- 各配一个专属技能:会议纪要 / 写PRD / 技术方案 / 工时评估 / 交互方案 / 测试用例。

**扩展提示词**
- 四处入口:身份卡编辑、主对话输入框、群聊输入框、群聊加成员时按名字现造身份卡。
- 只写「架构师」三个字也能点,它会补出角色边界、关注点、输出要求;
  「做个记账 APP」会补出场景、约束、验收标准、不做什么。
- 关键是扩展**有结构**:让模型「写详细点」得到的只是同一句话的注水版,
  所以每种场景都给死了产出骨架。

**六个通用工作技能**
- 结构化写作 / 画图表 / 查错 / 审查改动 / 拆任务 / 解释清楚。
- 不绑角色,任何身份、任何房间、主对话都能调。自建群聊加成员时可勾选,
  勾中的会连同「什么情况下该调哪个」一起写进身份卡。

**新增工具**
- `delete_file`、`make_directory` —— 此前只能靠 shell 拼 `rm`/`mkdir`,意图不显式。
- `download_file` —— 与 `web_fetch` 分工:抓正文进上下文,下载存字节**不进**上下文。
- `sleep` —— 轮询外部状态时的间隔。
- `get_memory_by_title` —— `recall_memory` 给的是截断摘要,认出是哪条后要取全文。

**界面**
- 主对话与群聊都加了对话气泡,自己的靠右、对方靠左。
- 群聊搬进侧边栏 —— 它是一种对话,和主对话、Goal 任务同一层,埋在设置里定位就错了。
- 身份卡分场景:群聊角色卡不再混进主对话的身份列表。

**重要修复**
- **打开即闪退**。`database` 在被赋值之前就被使用,`lateinit` 未初始化异常抛在
  `Application.onCreate` 里,进程当场死,全新安装一样崩。
- **数据库结构不一致导致的启动崩溃**(两处):迁移建了索引但实体没声明,Room 升级校验直接抛异常,
  且 `fallbackToDestructiveMigration` 救不了。已加自检测试防止再犯。
- **Markdown 只渲染代码块**,`**粗体**`、`# 标题`、`- 列表`、`` `行内代码` ``、`[链接]()`
  的符号全都原样显示在界面上。现在完整支持,含表格与可点链接。
- **中文 @ 判不出来**。边界规则要求 @ 前面是空白或括号,于是「他问你呢@前端设计师」
  判成不命中 —— 用户 @ 了没人应,群聊看着像卡死,而日志里什么错都没有。
- **群聊不记账**:群聊不走主客户端,完全绕过了用量记录 —— 聊半天烧掉一大笔,
  用量分析却显示「还没有用量记录」。
- 用量记账写库失败以前完全无声;只有缓存读、输入输出都是 0 的记录会被当空帧丢掉
  (Anthropic 缓存命中就长这样,那是真花过钱的)。
- 分配模型时只显示一个:可选模型只从「已启用」列表读,而那并不代表供应商提供了什么。
  现在可刷新拉取,也可手填。

## [1.06]

- **看板改造成智能体工作队列**。任务标为「就绪」后智能体自动领取执行,跑完停在「待验收」
  等你过目;可指派给指定子智能体;失败进「受阻」可重跑;保留每次执行记录;单任务超时
  自动判失败;应用被杀后正在跑的任务自动收回队列。
- **修正用量统计**:不同厂商对「输入 token 是否含缓存」口径不同,此前一律按同一种算,
  导致缓存被算两遍 —— 命中率被压到真实值的一半、成本虚高。
- **群聊历史改为按成员视角投影**:自己说过的话是 assistant、别人的是 user 带名字前缀。
  此前整段历史拼成一条消息,弱模型容易串台替别人发言。
- **删除配置环境改为先备份**,可从「最近删除」恢复 —— 此前手滑一下配置全没。

## [1.05]

- **每条消息可复制,可让 AI 重新回答**。
- **身份设定扩展**:简介、开场白、行为标记、可用工具白名单、独立供应商与模型。
- **群聊房间**:多个智能体同处一室,`@` 谁谁回答,历史过长自动压缩。
- **看板**:任务规划与状态流转,可把 AI 的计划一键固化成长期待办。
- **多配置环境**:配置完全隔离,可导出导入(导出文件**不含 API Key**)。
- **用量分析**:30 天趋势、模型分布、成本估算、缓存命中率。
- **日志查看**:应用日志与崩溃日志,按级别和关键词过滤。
- **局域网设备发现**:发现同一 WiFi 下的其他 XINCODE 设备。

## [1.04]

- **修「未知工具: null」死循环**。流式增量解析里,后续帧的空值会把已经拿到的工具名覆盖成
  字面字符串 `"null"`(`org.json` 的 `optString` 碰到 JSON null 返回的是 `"null"` 而不是默认值),
  于是模型反复调用一个不存在的工具。
- **附件去掉大小上限**,图片改走文件路径而非读进内存;顺带修了取消上限后暴露的主线程卡顿、
  大文本顶爆上下文、图片描述内存放大三个问题。
- **供应商能力开关**:识图 / 音频 / 视频 / 工具调用可逐项声明,不支持的能力不再硬发。
- **功能模型配置页**:11 个内部调用点(压缩、复盘、子智能体、裁判、定时…)可各自指定模型。
- **供应商配置支持手填模型 ID** —— 给拉不到 `/models` 列表的中转站用。
- 市场添加对话框支持拉取模型列表,可 OAuth 的供应商在卡片上标出。

## [1.03]

- **对话改为交错时间线**:说一段 → 做一步 → 再说一段,不再是「一大段文字 + 末尾一堆工具」。
  工具行显示中文动词标签(读取 / 执行 / 搜索网页…)。
- **修「做一点就罢工」与「回复老不回」**:SSE 流被中途掐断不再当成完整回复(现在会检测结束标记
  并自动续跑);模型请求也不会再永久挂起(补了兜底唤醒)。
- **修回复截断**:流式收集用 `collectLatest` 会在处理慢时丢弃未消费的片段,改回 `collect`。
- **修中途插话完全失效**(消息不显示 + AI 无响应),以及 `reasoning_effort` Max 档导致的 HTTP 400。
- 新增 Nous Portal 账号登录(OAuth 设备码);市场里 OAuth 类供应商改为登录授权,无需手填 API Key。
- 供应商市场补全:新增订阅套餐分区与十余个按量供应商。
- 新增独立关于页(图标 / 版本 / 检查更新 / 项目地址 / 许可)。

## [1.02]

**重要修复:多个供应商配不通(HTTP 400)**
- 修复 base_url 拼接:此前无脑追加 `/v1/chat/completions`,导致 base_url 自带版本段的供应商全部配不通——
  OpenAI、硅基流动、魔搭 ModelScope、OpenRouter、Moonshot、OpenCode Zen(带 `/v1`)会拼成 `/v1/v1/…`,
  智谱 GLM(带 `/v4`)会拼成 `/v4/v1/…`,**从「免费模型/供应商」市场一键添加它们基本都会失败**。
  现在改为版本段感知:base_url 已带 `/v1`、`/v4` 等版本段时只接资源路径,不带才补 `/v1`。
  拉取模型列表与配置页的地址预览使用同一规则。
- 4xx 错误不再被压成一句「HTTP 400」:现在原样透出供应商返回的具体原因(如 model not supported、
  unsupported parameter),用户和模型都能据此自我纠正。

**AI 时间感知**
- 修复 AI 把 UTC 当本地时间(本地 11 点被说成 3 点),进而把定时任务时间点算错的问题。
- 系统提示注入设备本地时间与时区;新增 `current_time` 工具(纯本地读时钟,不联网、不需 root)。

**闪退修复**
- `execute_code` 改用专用线程池:此前脚本内调用工具会用 runBlocking 阻塞 CPU 协程池,
  连续调用网络工具会耗尽线程池导致无响应被系统杀掉。
- 工具执行的三层异常捕获扩到 `Throwable`:此前 `StackOverflowError`、`OutOfMemoryError` 等
  属于 Error 而非 Exception,一律漏接直接崩溃。
- 新增全局崩溃兜底:后台线程的未捕获异常记录到 crash.log 后不再杀死整个进程。

**界面**
- 修复输入框工具栏溢出:协作+完全访问叠加较长模型名时,发送键会被挤出屏幕。现在模式与模型名
  过长时自动省略,发送键固定保留。
- 修复深色模式:18 个子页面绕过主题系统写死了浅色,切换深色后仍显示浅色。现已全部跟随主题
  (指挥室、终端等刻意固定的专用配色保持不变)。

**新增**
- 启动静默检查更新:发现新版本时弹窗显示更新说明并提供下载跳转;无网络/已是最新一律安静跳过,
  支持「跳过此版本」。
- GitHub 登录改为内置官方 OAuth App:点「登录 GitHub」即可授权,**不再需要用户自行注册 OAuth App**。

## [1.01]

**内置环境全面国内化**
- 内置 Ubuntu 环境的部署镜像源全部改为国内并实测可用:
  - Ubuntu base rootfs:清华 TUNA / 南京大学 / 北外多镜像互备(24.04、22.04)
  - apt 软件源:清华 ubuntu-ports;并把官方 base 自带的源就地改写为国内镜像
  - DNS:阿里(223.5.5.5)/ 腾讯(119.29.29.29)/ 百度(180.76.76.76)
  - pip:写全局 `/etc/pip.conf` 指向清华 PyPI
  - Rust/Cargo:rsproxy.cn(rustup-init、工具链、crates 索引)
  - Android 命令行工具:腾讯 AndroidSDK 镜像

**供应商配置页与旋转 bug 修复(社区反馈)**
- 新建/编辑配置页现在整页可上下滚动,「保存配置」按钮不再被下方的模型多选列表挡住(手机小屏、平板横屏都能正常保存)。
- 「启用模型(多选)」列表支持内部滚动,不会再出现列表加载不全、无法翻看/勾选后面模型的问题。
- 修复横竖屏切换会自动跳回聊天页的问题:旋转屏幕不再重建界面,当前所在页面与已填写的表单内容都会保留。

**中途插话不打断 + 回车发送开关**
- 新增「中途插话(不打断)」:AI 正在执行时,你可以直接在输入框打字发送,这句话会作为新指令注入到当前循环,AI 读到后自然衔接,而**不会打断/杀掉正在进行的工作**(底层复用 Hermes 打断-重定向机制)。运行中输入框不再被禁用;发送键在有输入时变为「注入」,无输入时才是「停止」。
- 新增「回车发送」开关(设置 → 外观):可选回车直接发送(换行用输入法组合键),或回车换行(发送靠 [→] 键),由你自己决定。

**Git OAuth 登录 + 免 root 远程 MCP**
- 新增 GitHub **OAuth 设备流登录**:点「登录 GitHub」→ 打开网页输入用户码授权 → 自动拿到 token,免手动去建 Personal Access Token(像 Operit 那样直接登录账户)。登录后自动回填用户名。需要在 GitHub 注册一个 OAuth App(勾选 Enable Device Flow)并填入其 Client ID(设备流 Client ID 可公开、无密钥泄露风险)。
- 新增一键添加 **GitHub 官方远程 MCP**(HTTP,`api.githubcopilot.com/mcp`):**不需要 root、不需要 node**,AI 直接用 GitHub API 管理仓库/PR/Issue/文件。原「本地 npx MCP」与「配置到 Linux 环境走终端 git」保留为可选路径。

## [1.0] - 首个公开正式版

XINCODE 是一个运行在 Android 上的自主 AI 智能体,纯 Kotlin 原生实现。首个公开版本包含以下能力:

### 智能体核心
- 多供应商接入:OpenAI 兼容 / DeepSeek / Anthropic 等,支持自定义端点与模型清单
- 工具调用循环 + 结构化输出 + Prompt 缓存纪律(前缀稳定、缓存命中优化)
- 计划模式(可视化任务卡)、协作模式(主脑把任务并行派发给子智能体)
- 身份卡(可切换的人格/系统提示 + 采样参数)

### 子智能体与「指挥室」
- `dispatch_agents` 并行调度多个专职子智能体,各用各的专属技能/工具,汇总回主脑
- 像素风「智能体指挥室」实时动画:每个子智能体一个工位与像素小人,派活即联动

### 环境与终端
- root 终端;内置 Ubuntu 环境(root + chroot 自动部署),可在其中执行命令
- 环境配置页,一键部署常用开发环境/工具

### 记忆
- 长期记忆:FTS 全文检索 + 向量语义检索;自动从对话中沉淀
- 跨对话记忆:普通对话之间互通、项目内对话按项目隔离
- 精编两文件记忆(用户画像 / 近况)冻结进系统提示,由后台复盘分身维护

### 上下文与成本
- 输入框旁上下文圆环(绿→蓝→黄→红),实时 token / 缓存命中显示
- 可配置上下文窗口与自动压缩阈值、自定义总结规则
- 成本显示(人民币,缓存感知)

### 效率与自动化
- 联网搜索:必应 / 百度 / 搜狗 / DuckDuckGo 多引擎融合,正文抓取
- 目标(Goal)/ 工作(Work)多任务模式:侧边栏独立对话、后台推进、完成通知
- 定时任务(cron / WorkManager)、语音转写、视觉与深度推理委托副模型

### 技能与 MCP
- 内置基础技能 + 技能管理;`/技能名` 主动调用
- MCP 支持(stdio 本地传输 + HTTP);`@服务器名` 优先使用其工具

### 安全
- 权限模式(询问 / 完全访问),敏感操作确认卡
- 审计日志