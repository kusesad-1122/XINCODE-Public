# XINCODE 正式版签名配置模板
# ----------------------------------------------------------------------
# 用法(全部在你自己的电脑上做,密钥永远不离开本机):
#   1. 复制本文件为 keystore.properties(同目录);
#   2. 若还没有签名密钥,先生成一个(有效期 ~27 年):
#        keytool -genkeypair -v -keystore xincode-release.jks \
#          -alias xincode -keyalg RSA -keysize 2048 -validity 10000
#   3. 把下面四项填成你的真实值;
#   4. 打签名包:  ./gradlew :app:assembleRelease
#      产物在  app/build/outputs/apk/release/app-release.apk
#
# 注意:keystore.properties 和 *.jks 都已在 .gitignore 里,绝不会被提交。
#       请自行【离线备份】.jks 和这几个密码——一旦丢失将无法再给同一个应用发布更新。
# ----------------------------------------------------------------------
storeFile=xincode-release.jks
storePassword=你的密钥库口令
keyAlias=xincode
keyPassword=你的密钥口令
